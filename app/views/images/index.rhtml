<h1>Listing images</h1>
<h3>Warning !!!  don't delete an image who's in a page, or it won't be displayed anymore... </h3>
<table>
  <tr>
  </tr>
  
<% for image in @images %>
  <tr>
    <td><%= image_tag( image.public_filename(:thumb) ) %></td>
    <td><%= link_to 'Show', my_image_path(image) %></td>
    <td><%= link_to 'Destroy', my_image_path(image), :confirm => 'Are you sure?', :method => :delete %></td>
  </tr>
<% end %>
</table>

<br />

<%= link_to 'New image', new_my_image_path %><br />
